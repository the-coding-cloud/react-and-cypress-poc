{
  "name": "cypress_poc",
  "version": "1.0.1",
  "description": "Cypress PoC",
  "main": "index.js",
  "scripts": {
    "cy:run": "cypress run --browser chrome",
    "cy:open": "cypress open",
    "cy:test": "cy2 run --parallel --record --key xxx --ci-build-id $CI_BUILD_ID --config-file $CONFIG_FILE"
  },
  "author": "Oana N",
  "devDependencies": {
    "cy2": "^2.0.0",
    "cypress": "9.5.4",
    "@faker-js/faker": "^6.2.0",
    "mocha": "^9.2.2",
    "mocha-teamcity-reporter": "^4.2.0",
    "typescript": "^4.6.3"
  }
}
